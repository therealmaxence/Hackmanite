{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Hackmanite NLP Service","text":"<p>This is the FastAPI backend service that powers Hackmanite. It provides Named Entity Recognition (NER), OCR processing, document parsing, and interacts with KuzuDB (an embedded graph database).</p>"},{"location":"#submodules-capabilities","title":"Submodules &amp; Capabilities","text":"<ol> <li>API Routers (<code>routers/</code>):</li> <li><code>extract</code>: Controls file extraction jobs, dispatching tasks to text extraction and regex extractors.</li> <li><code>graph</code>: Handles reading and writing entities and relations to KuzuDB.</li> <li> <p><code>tesseract</code>: Endpoint for raw OCR requests.</p> </li> <li> <p>Parsing &amp; Ingestion Services (<code>services/</code>):</p> </li> <li><code>dispatcher</code>: Orchestrates multi-tier ingestion (metadata -&gt; text -&gt; OCR).</li> <li><code>entity_extraction</code>: Runs spaCy models to identify names, locations, organizations, dates, etc.</li> <li><code>ocr</code>: Wraps Tesseract for image-based text recovery.</li> <li><code>email_parser</code>: Extracts structured email headers, dates, and thread parent/child links from <code>.eml</code> files.</li> </ol>"},{"location":"#running-locally","title":"Running Locally","text":"<p>To run the documentation dev server locally:</p> <pre><code>pip install -r requirements-dev.txt\nmkdocs serve\n</code></pre> <p>And to build the static production site:</p> <pre><code>mkdocs build\n</code></pre> <p>This will generate a static folder <code>site/</code> at the root of <code>nlp-service</code>.</p>"},{"location":"api/","title":"API &amp; Module Reference","text":"<p>This section provides auto-generated API references for the FastAPI endpoints and core pipeline services.</p>"},{"location":"api/#main-entrypoint","title":"Main Entrypoint","text":""},{"location":"api/#main","title":"<code>main</code>","text":""},{"location":"api/#routers","title":"Routers","text":""},{"location":"api/#document-extraction-router","title":"Document Extraction Router","text":""},{"location":"api/#routers.extract","title":"<code>routers.extract</code>","text":""},{"location":"api/#graph-database-router","title":"Graph Database Router","text":""},{"location":"api/#routers.graph","title":"<code>routers.graph</code>","text":""},{"location":"api/#routers.graph.clear_graph","title":"<code>clear_graph()</code>  <code>async</code>","text":"<p>Clear all data from KuzuDB (detach delete all nodes and edges).</p> Source code in <code>routers\\graph.py</code> <pre><code>@router.delete(\"\", response_model=dict)\nasync def clear_graph():\n    \"\"\"Clear all data from KuzuDB (detach delete all nodes and edges).\"\"\"\n    with kuzu_db._write_lock:\n        try:\n            conn = kuzu_db.get_write_conn()\n            conn.execute(\"MATCH (n:Entity) DETACH DELETE n\")\n            conn.execute(\"MATCH (f:FileRef) DETACH DELETE f\")\n            return {\"success\": True}\n        except Exception as exc:\n            logger.error(\"clear_graph failed: %s\", exc, exc_info=True)\n            raise HTTPException(status_code=500, detail=str(exc))\n</code></pre>"},{"location":"api/#routers.graph.delete_file","title":"<code>delete_file(file_id)</code>  <code>async</code>","text":"<p>Delete all occurrences and relationships associated with this file in KuzuDB.</p> Source code in <code>routers\\graph.py</code> <pre><code>@router.delete(\"/file/{file_id}\")\nasync def delete_file(file_id: str):\n    \"\"\"Delete all occurrences and relationships associated with this file in KuzuDB.\"\"\"\n    try:\n        kuzu_db.delete_file_ref(file_id)\n        return {\"success\": True}\n    except Exception as exc:\n        logger.error(\"delete_file failed: %s\", exc, exc_info=True)\n        raise HTTPException(status_code=500, detail=str(exc))\n</code></pre>"},{"location":"api/#routers.graph.delete_files","title":"<code>delete_files(req)</code>  <code>async</code>","text":"<p>Delete all occurrences and relationships associated with multiple files in KuzuDB.</p> Source code in <code>routers\\graph.py</code> <pre><code>@router.post(\"/files/delete\", response_model=dict)\nasync def delete_files(req: DeleteFilesRequest):\n    \"\"\"Delete all occurrences and relationships associated with multiple files in KuzuDB.\"\"\"\n    try:\n        kuzu_db.delete_files_ref(req.file_ids)\n        return {\"success\": True}\n    except Exception as exc:\n        logger.error(\"delete_files failed: %s\", exc, exc_info=True)\n        raise HTTPException(status_code=500, detail=str(exc))\n</code></pre>"},{"location":"api/#routers.graph.delete_node","title":"<code>delete_node(node_id, file_ids=Query(..., description='Comma-separated file IDs to delete occurrences in'))</code>  <code>async</code>","text":"<p>Delete node occurrences and connections in the given files in KuzuDB.</p> Source code in <code>routers\\graph.py</code> <pre><code>@router.delete(\"/node/{node_id}\")\nasync def delete_node(node_id: str, file_ids: str = Query(..., description=\"Comma-separated file IDs to delete occurrences in\")):\n    \"\"\"Delete node occurrences and connections in the given files in KuzuDB.\"\"\"\n    if ids := [f.strip() for f in file_ids.split(\",\") if f.strip()]:\n        try:\n            kuzu_db.delete_entity_occurrences(node_id, ids)\n        except Exception as exc:\n            logger.error(\"delete_node failed: %s\", exc, exc_info=True)\n            raise HTTPException(status_code=500, detail=str(exc))\n    return {\"success\": True}\n</code></pre>"},{"location":"api/#routers.graph.get_edges","title":"<code>get_edges(node_ids=Query(..., description='Comma-separated entity IDs to find edges between'))</code>  <code>async</code>","text":"<p>Return all CO_OCCURS edges where both endpoints are in the given node set.</p> Source code in <code>routers\\graph.py</code> <pre><code>@router.get(\"/edges\", response_model=EdgesResponse)\nasync def get_edges(node_ids: str = Query(..., description=\"Comma-separated entity IDs to find edges between\")):\n    \"\"\"Return all CO_OCCURS edges where both endpoints are in the given node set.\"\"\"\n    ids = [n.strip() for n in node_ids.split(\",\") if n.strip()]\n    if len(ids) &lt; 2:\n        return EdgesResponse(edges=[])\n    try:\n        return EdgesResponse(edges=[EdgeOut(**e) for e in kuzu_db.get_edges_for_nodes(ids)])\n    except Exception as exc:\n        logger.error(\"get_edges failed: %s\", exc, exc_info=True)\n        raise HTTPException(status_code=500, detail=str(exc))\n</code></pre>"},{"location":"api/#routers.graph.get_edges_post","title":"<code>get_edges_post(req)</code>  <code>async</code>","text":"<p>Return all CO_OCCURS edges where both endpoints are in the given node set (POST version).</p> Source code in <code>routers\\graph.py</code> <pre><code>@router.post(\"/edges\", response_model=EdgesResponse)\nasync def get_edges_post(req: EdgesPostRequest):\n    \"\"\"Return all CO_OCCURS edges where both endpoints are in the given node set (POST version).\"\"\"\n    if len(req.node_ids) &lt; 2:\n        return EdgesResponse(edges=[])\n    try:\n        return EdgesResponse(edges=[EdgeOut(**e) for e in kuzu_db.get_edges_for_nodes(req.node_ids)])\n    except Exception as exc:\n        logger.error(\"get_edges_post failed: %s\", exc, exc_info=True)\n        raise HTTPException(status_code=500, detail=str(exc))\n</code></pre>"},{"location":"api/#routers.graph.get_neighbors","title":"<code>get_neighbors(node_id=Query(..., description='Entity ID to expand'), loaded_ids=Query(None, description='Comma-separated IDs already in the graph (to skip)'))</code>  <code>async</code>","text":"<p>Return 1-hop neighbors of a node that aren't already in the graph.</p> Source code in <code>routers\\graph.py</code> <pre><code>@router.get(\"/neighbors\", response_model=NeighborsResponse)\nasync def get_neighbors(\n    node_id: str = Query(..., description=\"Entity ID to expand\"),\n    loaded_ids: Optional[str] = Query(None, description=\"Comma-separated IDs already in the graph (to skip)\"),\n):\n    \"\"\"Return 1-hop neighbors of a node that aren't already in the graph.\"\"\"\n    already_loaded = [i.strip() for i in loaded_ids.split(\",\") if i.strip()] if loaded_ids else []\n    try:\n        return _to_neighbors_response(*kuzu_db.get_neighbors(node_id, already_loaded))\n    except Exception as exc:\n        logger.error(\"get_neighbors failed: %s\", exc, exc_info=True)\n        raise HTTPException(status_code=500, detail=str(exc))\n</code></pre>"},{"location":"api/#routers.graph.get_neighbors_post","title":"<code>get_neighbors_post(req)</code>  <code>async</code>","text":"<p>Return 1-hop neighbors of a node that aren't already in the graph (POST version).</p> Source code in <code>routers\\graph.py</code> <pre><code>@router.post(\"/neighbors\", response_model=NeighborsResponse)\nasync def get_neighbors_post(req: NeighborsPostRequest):\n    \"\"\"Return 1-hop neighbors of a node that aren't already in the graph (POST version).\"\"\"\n    try:\n        return _to_neighbors_response(*kuzu_db.get_neighbors(req.node_id, req.loaded_ids or []))\n    except Exception as exc:\n        logger.error(\"get_neighbors_post failed: %s\", exc, exc_info=True)\n        raise HTTPException(status_code=500, detail=str(exc))\n</code></pre>"},{"location":"api/#routers.graph.get_node","title":"<code>get_node(node_id)</code>  <code>async</code>","text":"<p>Retrieve a single entity node's properties from KuzuDB by ID.</p> Source code in <code>routers\\graph.py</code> <pre><code>@router.get(\"/node/{node_id}\", response_model=NodeDetailsOut)\nasync def get_node(node_id: str):\n    \"\"\"Retrieve a single entity node's properties from KuzuDB by ID.\"\"\"\n    try:\n        if not (node := kuzu_db.get_node_by_id(node_id)):\n            raise HTTPException(status_code=404, detail=\"Entity not found in graph\")\n        return NodeDetailsOut(**node)\n    except HTTPException:\n        raise\n    except Exception as exc:\n        logger.error(\"get_node failed: %s\", exc, exc_info=True)\n        raise HTTPException(status_code=500, detail=str(exc))\n</code></pre>"},{"location":"api/#routers.graph.get_nodes","title":"<code>get_nodes(file_ids=Query(..., description='Comma-separated file IDs'), limit=Query(50, ge=1, le=500), offset=Query(0, ge=0), types=Query(None, description='Comma-separated entity types to filter'))</code>  <code>async</code>","text":"<p>Return a paginated batch of top entities (by occurrence).</p> Source code in <code>routers\\graph.py</code> <pre><code>@router.get(\"/nodes\", response_model=NodesResponse)\nasync def get_nodes(\n    file_ids: str = Query(..., description=\"Comma-separated file IDs\"),\n    limit: int = Query(50, ge=1, le=500),\n    offset: int = Query(0, ge=0),\n    types: Optional[str] = Query(None, description=\"Comma-separated entity types to filter\"),\n):\n    \"\"\"Return a paginated batch of top entities (by occurrence).\"\"\"\n    ids = [f.strip() for f in file_ids.split(\",\") if f.strip()]\n    if not ids:\n        return NodesResponse(nodes=[], total=0, offset=offset, has_more=False)\n    type_filter = [t.strip() for t in types.split(\",\") if t.strip()] if types else None\n    try:\n        rows = kuzu_db.get_top_nodes(ids, limit=limit, offset=offset, type_filter=type_filter)\n        return _to_nodes_response(rows, kuzu_db.get_node_count(ids), limit, offset)\n    except Exception as exc:\n        logger.error(\"get_nodes failed: %s\", exc, exc_info=True)\n        raise HTTPException(status_code=500, detail=str(exc))\n</code></pre>"},{"location":"api/#routers.graph.get_nodes_post","title":"<code>get_nodes_post(req)</code>  <code>async</code>","text":"<p>Return a paginated batch of top entities (POST version).</p> Source code in <code>routers\\graph.py</code> <pre><code>@router.post(\"/nodes\", response_model=NodesResponse)\nasync def get_nodes_post(req: NodesPostRequest):\n    \"\"\"Return a paginated batch of top entities (POST version).\"\"\"\n    if not req.file_ids:\n        return NodesResponse(nodes=[], total=0, offset=req.offset, has_more=False)\n    type_filter = [t.strip() for t in req.types.split(\",\") if t.strip()] if req.types else None\n    try:\n        rows = kuzu_db.get_top_nodes(req.file_ids, limit=req.limit, offset=req.offset, type_filter=type_filter)\n        return _to_nodes_response(rows, kuzu_db.get_node_count(req.file_ids), req.limit, req.offset)\n    except Exception as exc:\n        logger.error(\"get_nodes_post failed: %s\", exc, exc_info=True)\n        raise HTTPException(status_code=500, detail=str(exc))\n</code></pre>"},{"location":"api/#routers.graph.import_graph","title":"<code>import_graph(req)</code>  <code>async</code>","text":"<p>Bulk import a graph session into KuzuDB using a single transaction.</p> Source code in <code>routers\\graph.py</code> <pre><code>@router.post(\"/import\", response_model=dict)\nasync def import_graph(req: ImportGraphRequest):\n    \"\"\"Bulk import a graph session into KuzuDB using a single transaction.\"\"\"\n    try:\n        return kuzu_db.bulk_import_transaction(file_ids=req.file_ids, nodes=req.nodes, edges=req.edges)\n    except Exception as exc:\n        logger.error(\"import_graph failed: %s\", exc, exc_info=True)\n        raise HTTPException(status_code=500, detail=str(exc))\n</code></pre>"},{"location":"api/#routers.graph.node_count","title":"<code>node_count(file_ids=Query(..., description='Comma-separated file IDs'))</code>  <code>async</code>","text":"<p>Return total number of distinct entities across the given files.</p> Source code in <code>routers\\graph.py</code> <pre><code>@router.get(\"/node-count\", response_model=dict)\nasync def node_count(file_ids: str = Query(..., description=\"Comma-separated file IDs\")):\n    \"\"\"Return total number of distinct entities across the given files.\"\"\"\n    ids = [f.strip() for f in file_ids.split(\",\") if f.strip()]\n    if not ids:\n        return {\"count\": 0}\n    try:\n        return {\"count\": kuzu_db.get_node_count(ids)}\n    except Exception as exc:\n        logger.error(\"node-count failed: %s\", exc, exc_info=True)\n        raise HTTPException(status_code=500, detail=str(exc))\n</code></pre>"},{"location":"api/#routers.graph.node_count_post","title":"<code>node_count_post(req)</code>  <code>async</code>","text":"<p>Return total number of distinct entities across the given files (POST version).</p> Source code in <code>routers\\graph.py</code> <pre><code>@router.post(\"/node-count\", response_model=dict)\nasync def node_count_post(req: NodeCountPostRequest):\n    \"\"\"Return total number of distinct entities across the given files (POST version).\"\"\"\n    if not req.file_ids:\n        return {\"count\": 0}\n    try:\n        return {\"count\": kuzu_db.get_node_count(req.file_ids)}\n    except Exception as exc:\n        logger.error(\"node-count post failed: %s\", exc, exc_info=True)\n        raise HTTPException(status_code=500, detail=str(exc))\n</code></pre>"},{"location":"api/#routers.graph.recompute_tfidf","title":"<code>recompute_tfidf(req)</code>  <code>async</code>","text":"<p>Update TF-IDF values for occurrences in KuzuDB.</p> Source code in <code>routers\\graph.py</code> <pre><code>@router.post(\"/recompute-tfidf\", response_model=dict)\nasync def recompute_tfidf(req: RecomputeTfidfRequest):\n    \"\"\"Update TF-IDF values for occurrences in KuzuDB.\"\"\"\n    try:\n        kuzu_db.update_tfidf_properties([dict(u) for u in req.updates])\n        return {\"success\": True}\n    except Exception as exc:\n        logger.error(\"recompute_tfidf failed: %s\", exc, exc_info=True)\n        raise HTTPException(status_code=500, detail=str(exc))\n</code></pre>"},{"location":"api/#routers.graph.run_query","title":"<code>run_query(req)</code>  <code>async</code>","text":"<p>Execute an arbitrary read-only Cypher query against KuzuDB.</p> Source code in <code>routers\\graph.py</code> <pre><code>@router.post(\"/query\", response_model=dict)\nasync def run_query(req: QueryRequest):\n    \"\"\"Execute an arbitrary read-only Cypher query against KuzuDB.\"\"\"\n    if _WRITE_KEYWORDS.search(req.query):\n        raise HTTPException(status_code=400, detail=\"Only read-only queries are permitted on this endpoint\")\n    try:\n        return {\"rows\": kuzu_db.execute_read_query(req.query, req.params)}\n    except Exception as exc:\n        logger.error(\"run_query failed: %s\", exc, exc_info=True)\n        raise HTTPException(status_code=500, detail=str(exc))\n</code></pre>"},{"location":"api/#ocr-router","title":"OCR Router","text":""},{"location":"api/#routers.tesseract","title":"<code>routers.tesseract</code>","text":""},{"location":"api/#core-services","title":"Core Services","text":""},{"location":"api/#ingestion-dispatcher","title":"Ingestion Dispatcher","text":""},{"location":"api/#services.dispatcher","title":"<code>services.dispatcher</code>","text":""},{"location":"api/#entity-extraction-service","title":"Entity Extraction Service","text":""},{"location":"api/#services.entity_extraction","title":"<code>services.entity_extraction</code>","text":""},{"location":"api/#ocr-service","title":"OCR Service","text":""},{"location":"api/#services.ocr","title":"<code>services.ocr</code>","text":""},{"location":"api/#email-ingestion-parser","title":"Email Ingestion Parser","text":""},{"location":"api/#services.email_parser","title":"<code>services.email_parser</code>","text":""}]}
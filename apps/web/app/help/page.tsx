import HelpClient from '@/components/pages/HelpClient';

export const metadata = {
  title: 'Help Center | Hackmanite',
  description: 'Learn about Hackmanite features, algorithms, filters, and score calculation methodologies.',
};

export default function HelpPage() {
  return <HelpClient />;
}

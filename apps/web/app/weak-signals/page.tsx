import WeakSignalsClient from '@/components/pages/WeakSignalsClient';

export const metadata = {
  title: 'Weak Signals | Hackmanite',
  description: 'Identify and analyze emerging, bridging, and niche signals across session entity graphs.',
};

export default function WeakSignalsPage() {
  return <WeakSignalsClient />;
}
